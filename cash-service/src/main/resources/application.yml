server:
  port: ${SERVER_PORT:8090}

spring:
  application:
    name: cash-service

  config:
    import: 'kubernetes:'

  cloud:
    kubernetes:
      config:
        enabled: true
        name: ${spring.application.name}
        namespace: default
        fail-fast: true
        include-profile-specific-sources: false